env: "production"

site: "ap1.datadoghq.com"

apm_config:
  enabled: true
  non_local_traffic: true
  receiver_port: 8126

tags:
  - service: vspo-portal-cron
  - version: 1.0.0
  - env: production

otlp_config:
  metrics_enabled: true
  receiver:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
      http:
        endpoint: 0.0.0.0:4318

logs_enabled: true